# Copy to .env in this folder (do not commit secrets).
OPENAI_API_KEY=

# Optional: folder of PDFs (defaults to ../data relative to assignment root).
# SDG_DATA_ROOT=C:\path\to\pdfs

# Optional: comma-separated CORS origins (default *).
# SDG_CORS_ORIGINS=http://127.0.0.1:5173
